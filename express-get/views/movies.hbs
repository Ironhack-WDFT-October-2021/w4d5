<h1>{{heading}}</h1>

<form action="/moviesearch" method="GET">
	<label for="search">Enter a Title:</label>
	<input type="text" id="search" name="q">
	<button type="submit">Search</button>
</form>

{{#each movieList}}

	{{!-- we now add a reference to partials/movieCard --}}
	{{!-- {{> <name of the partial file> <the data>}} --}}
	{{> movieCard this}}


	{{!-- <a href="/godfather">
		<img src="{{poster}}" alt="poster for {{title}}">
	</a>

	<h2>
		{{title}}
	</h2>
	<p>
		{{year}}
	</p>
	<p>
		{{director}}
	</p>
	<ul>
		{{#each genre}}
			<li>{{this}}</li>
		{{/each}}
	</ul> --}}
{{/each}}